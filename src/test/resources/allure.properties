# Allure report output directory
allure.results.directory=target/allure-results

# Enable detailed logging for steps
allure.step.retry.policy.duration=0

# This configuration ensures that Allure generates detailed step-by-step reports.